name: essential_widgets
description: A humble library of widgets for Flutter.

# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.2+1

repository: https://github.com/Daniel324a/Essential_Widgets

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # assets:
  #   - assets/